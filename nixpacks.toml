[variables]
NIXPACKS_NODE_VERSION="20"
NIXPACKS_NO_CACHE="1"

[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm install --global yarn"]

[phases.build]
cmds = ["yarn install", "npx expo export --platform web"]

[start]
cmd = "npx serve -s dist -l 3002"
